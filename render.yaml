services:
  - type: web
    name: fedora-gnome-vnc-ngrok
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    autoDeploy: true
    envVars:
      - key: PORT
        value: 5901
